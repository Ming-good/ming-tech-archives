## 람다 기초
> **2025.06.24**
1. [[람다가 필요한 이유]]
	- 값 매개변수화
	- 동작 매개변수화
2. [[함수형 인터페이스란]] : 단 하나의 추상메서드를 가지는 인터페이스
> **2025.06.26**
3. [[함수 VS 메서드]]
	- 함수 : 독립적으로 존재
	- 메서드 : 클래스에 속해있는 함수이다.
	- 메서드도 함수이다. 다만 굳이 구분하자면 나누어지는 것일 뿐
4. [[익명 클래스를 람다로]]
	- 람다의 기본 형태 
> **2025.06.28**
5. [[람다의 정의]]
	- 람다와 람다식 차이
6. [[람다 표현식 생략]]
	- 기본
	- 단일 표현식일 경우 중괄호 생략
	- 매개변수 타입 추론 가능
	- 매개변수가 하나일 때 매개변수 괄호 생략 가능
7. [[고차 함수]]
	- 고차 함수란?
	- 고차 함수라는 용어가 생긴 유래
## 함수형 인터페이스
1.  [[함수형 인터페이스와 제네릭]]
	- 코드 재사용성과 타입 안정성 확보
>**2025.06.29**
2. [[기본 함수형 인터페이스]]
	- `Function` : 입력O, 반환O
	- `Consumer` : 입력O, 반환X
	- `Supplier` : 입력X, 반환O
	- `Runnable` : 입력X, 반환X
3. [[특화 함수형 인터페이스]] - 의미의 명시성, 가독성과 유지보수성을 위해 사용
	- `Predicate<T>` : 검사, 필터링
	- `UnaryOperator<T>` : 단항 연산
	- `BinaryOperator<T>` : 이항 연산
4. [[기타 함수형 인터페이스]]
	- BiXxx 시리즈 함수형 인터페이스
	- 기본형 지원 함수형 인터페이스 : 람다 사용시 성능 개선
## 람다 활용
1. [[제네릭 Filter 만들기]]
	- `Predicate<T>` 사용
2. [[Map 만들기]]
	- `Function<T, R>`사용
3. [[repeat]] : jdk11에 추가된 같은 문자 반복 메서드
> **2025.07.01**
4. [[명령형 vs 선언적 프로그래밍]]
	- 명령어 프로그래밍 : 어떻게
	- 선언적 프로개르밍 : 무엇을
5. [[정적 팩토리 메서드]]
	- 특징
	- 사용 예시
6. [[내부 반복 vs 외부 반복]]
	- 외부 반복: `for` 문, `while` 문과 같은 반복문 직접 사용
	- 내부 반복: `forEach()` 메서드로 데이터를 처리하는 방식
	- 외부 반복 VS 내부 반복 선택
> **2025.07.03**
## [[람다 vs 익명클래스]]
- 상속 관계
- this 키워드의 의미
- 캡처링
- 익명 클래스와 람다의 용도 구분
## 메서드 참조
1. [[메서드 참조란]]
	- 메서드 참조란 무엇인지
	- 메서드 참조는 브릿지 역할일 때 사용하면 직관적이고 간결한 코드를 작성할 수 있다.
	- 메서드 참조의 장점
		- 간결, 가독
		- 매개변수 명시적 작성 필요가 없음
		- 별도 로직 분리로 재사용성 상승
> **2025.07.06**
2. [[메서드 참조의 4가지 유형]]
	- 정적 메서드 참조: `Class::method`
	- 특정 객체의 인스턴스 메서드 참조: `obj::method`
	- 생성자 참조: `Class::new`
	- 임의 객체의 인스턴스 메서드 참조: `Class::method`
		- `Function<Class, String>`: 매개변수 없는 메서드
		- `BiFunction<Class, Integer, String>`: 매개변수 있는 메서드
	- (특정 VS 임의) 객체의 인스턴스 메서드 참조 차이점
		- 특정: 선언시점 인스턴스 고정
		- 임의: 실행시점 인스턴스 고정
## 스트림 API - 기본
> **2025.07.07**
1. [[스트림 API란]]
	- 스트림 정의
	- 스트림 특징
		- 데이터 소스를 변경하지 않음
		- 일회성 소비
		- 파이프라인 구성
		- 지연연산
		- 병렬처리 용이
2. [[지연 연산과 최적화]]
	- 지연 연산 정의
	- 지연 연산 이점
		- 단축평가
		- 메모리 사용 효율
		- 파이프라인 최적화
## 스트림 API - 기능
1. [[Stream 생성 방법]]
	- `List.of().stream()`
	- `Arrays.stream(arr)`
	- `Stream.of("a", "b", "c")`
	- `Stream.iterate(0, n -> n + 2)`
	- `Stream.generate(Math:random)`
2. [[중간 연산]] API 정리
3. [[FlatMap]]
	- flatMap 사용법
	- map과 flatMap 차이점
> **2025.07.09**
4.  [[최종 연산]] API 정리
> **2025.07.13**
5. [[기본형 특화 스트림]]
	- 기본형 특화 스트림의 종류(Int, Long, Double Stream)
	- 범위 생성 메서드
	- 주요 메서드
6. [[다운 스트림 컬렉터]]
	- 다운 스트림이 필요한 이유 : 그룹화 이후 후처리를 위해서 사용
	- 다운 스트림 컬렉터 기능(메서드)표
	- [[다운 스트림 컬렉터 예제 - 집계 기본]]
		- `groupingBy() VS partitioningBy()`
	- [[다운 스트림 컬렉터 예제 - 집계 활용]]
		- `mapping()` vs `collectingAndThen()`
> **2025.07.15**
## Optional
1. [[옵셔널이 필요한 이유]]
2. [[Optional 생성과 값 획득]]
	- Optional 생성 메서드
	- 값 획득 메서드
	- `isPresent()`와 `get()` 사용으로 값을 획득하기보다 `orElse()` 사용 권장
3. 