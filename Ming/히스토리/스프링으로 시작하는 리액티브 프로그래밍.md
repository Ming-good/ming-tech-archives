## 리액티브 시스템 이해하기
1. [[리액티브 시스템]]
	- MEANS: 메시지 기반
	- FORM: 회복, 탄력성
	- VALUE: 메시지 기반 통신을 기반으로 즉각적인 응답이 가능한 시스템을 만들 수 있는 가치
## 리액티브 프로그래밍
1. [[리액티브 프로그래밍]]
	- 특징
	- 구성
		- `Publisher`
		- `Subscriber`
		- `Data Source`
		- `Operator`
2. [[리액티브 스트림즈 구성요소]]
	- `Publisher`
	- `Subscriber`
	- `Subscription`
	- `Processor`
3. [[리액티브 스트림즈 용어 정의]]
	- Signal
	- Emit
	- Demand
	- Upstream / Downstream
	- Sequence
	- Operator
	- Source
	- Publisher 구현을 위한 기본 규칙
	- Subscriber 구현을 위한 기본 규칙
	- Subscription 구현을 위한 기본 규칙
## Blocking I/O와 Non-Blocking I/O
1. [[Blocking IO의 특징]]: 
	- 특징
	- 컨텍스트 스위칭 전환비용 문제
2. [[Non-Blocking IO 특징]]:  
	- 특징
	- CPU를 많이 사용할 경우 다른 스레드가 CPU를 점유해서 이벤트 루프  스레드의 처리를 지연 시킴
## Reactor 구성요소
1. [[Reactor란]]
	- 리액터란 무엇인가
	- 특징
2. Reactor 연산자
	1. [[생성 연산자]]
		- `jsut()`
		- `justOrEmpty()`
		- `empty()`
		- `fromArray()`
		-  `fromIterable()`
	2. [[중간 연산자]]
		- `concatWith()`
		- `concat()`
		- `delayElements()`
		- `share()`
		- `cache()`
		- `doOnNext()`
		- `doOnSubscribe()`
	3. [[최종 연산자]]
		- `subscribe()`
## Reactive Sequence (파이프라인)

#### Reactor Cold & Hot
1. [[Cold와 Hot 개념]]
	- Cold Sequence: 구독하면 처음부터 다시 emit
	- Hot Sequence: 구독시점부터 데이터 emit 받을 수 있음
2. [[Reactor에서 Hot의 두 가지 의미]]
	- Warm up: `cache()`
	- Hot: `share()`
#### 백프레셔
1. [[리액터에 Backpressure란]]
	- `BaseSubscriber()` 사용 예시
2. [[Backpressure 전략]]
	- ERROR 전략
	- DROP 전략
	- LATEST 전략
	- BUFFER 전략
		- DROP_LATEST 전략
		- DROP_OLDEST 전략
#### [[Sinks란]]
	- Sinks란?
	- `Sinks.One()`
	- `Sinks.Many()`
		- `unicast()`
		- `multicast()`
		- `replay()`
			- `limit()`
			- `all()`
##  스레드
1. [[Scheduler(스레드 관리 연산자)]]
	- `subscribeOn()`
	- `publishOn()`
	- `parallel().runOn()`
2. [[Schedulers 종류]]
	- `Schedulers.immediate()`
	- `Schedulers.single()`
	- `Schedulers.NewSingle()`
	- `Schedulers.boundedElastic()`
	- `Schedulers.parallel()`
	- `Schedulers.newXXXX()` 시리즈
## 리액터 Context
- [[Context란]]
	- Context 특징
		- 구독이 발생할 때마다 하나의 Context가 구독에 연결
		- Context는 아래에서 위로 전파된다.
		- Context가 데이터를 읽어오는 2가지 방식
			- 처음
			- 중간
	- Context 사용 예시
		- 서로 다른 스레드 간에 Context 공유 예시
		- Context의 전파 방식
		- 내부 Context와 외부 Context
- [[Context API]]
	- Context에서 자주 사용되는 API리스트