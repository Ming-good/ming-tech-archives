1. 제1정규화 : 테이블이 원자값을 갖도록 분해하는 것
   
2. 제2정규화 : 완전 함수 종속을 만족하도록 분해할 것 (함수 종속 이라느 기본키의 부분집합이 결
   정자가 되어선 안된다는 것)
   
3. 제3정규화 : 이행종속 제거 A->B->C 라면 A->C 가 성립 되는 것
   
4. BCNF정규화 : 제3 정규화를 진행한 테이블에 모든 결정자가 후보키가 되도록 테이블을 분해하는 것