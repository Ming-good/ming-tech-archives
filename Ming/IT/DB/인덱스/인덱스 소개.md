## 인덱스 구조
인덱스는 특정 칼럼의 데이터를 기반으로 생성되는, 원본 테이블과는 별개의 특수한 자료 구조이다.
- 인덱스는 지정된 칼럼의 값과, 해당 값을 가진 실제 데이터 행의 위치를 한 쌍으로 저장한다.
- 인덱스 내부의 데이터는 항상 정렬된 상태를 유지한다.

## 트리 자료 구조
#### 이진 탐색 트리 특징
- 데이터를 입력하는 시점에 정렬해서 보관한다.
- 작은 값은 왼쪽에 큰 값은 오른쪽에 저장한다.

#### 이진 탐색 트리 - 검색
![[Pasted image 20251004171529.png]]
- 약 1024개의 데이터를 단 10번의 계산으로 원하는 결과를 찾을 수 있다. 데이터의 크기가 늘어나도 늘어난 만큼 한 번의 계산에 남은 절반을 날려버리기 때문에 데이터의 크기가 클 수록 효과적이다.

#### 이진 탐색 트리 - 순회
- 이진 탐색 트리의 핵심은 입력 순서가 아니라, 데이터의 값을 기준으로 정렬해서 보관한다.

## 밸런스 트리
- 이진 탐색 트리의 경우 최악의 공우 `O(n)`만큼 성능이 나오게된다. 
- ![[Pasted image 20251008215238.png]]
- 이렇게 오른쪽으로 치우친 경우 15를 검색 했을 때 5만큼 검색을 해야한다.
#### 이진 탐색 트리 개선
- 이진 탐색 트리의 균형이 깨진 경우 균형을 맞추는 트리를 밸런스 트리라고한다.
- ![[Pasted image 20251008215412.png]]

## 실제 인덱스에 사용되는 트리
- 이진 탐색 트리는 검색에 `O(log n)` 의 성능을 제공하지만, 데이터가 순차적으로 입력되면 한쪽으로 치우쳐지는 문제 (Worst Case `O(n)` )가 발생할 수 있다. 
- 이 문제를 해결하기 위해 밸런스 트리(Balanced Tree) 개념이 도입되었고,
- B-트리는 이러한 밸런스 트리의 한 종류이다.
- B-트리가 특별히 중요한 이유는 단순히 균형을 유지하는 것을 넘어서 디스크 I/O횟수를 줄이는데 효율적이다.
	- 이진 탐색 트리는 한 노드가 하나의 데이터만 가지고 있어서 데이터를 검색할 때 여러 노드를 방문해야하는 문제가 있다.
	- B-트리는 하나의 노드가 여러 개의 자식 노드를 가질 수 있고 더 많은 데이터를 저장할 수 있도로 설계되어있어서 I/O 횟수를 획기적으로 줄여준다.