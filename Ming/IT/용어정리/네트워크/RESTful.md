|                    |                                                   |
| ------------------ | ------------------------------------------------- |
| 1. 자원(Resource) 중심 | `/users/123`, `/posts/1/comments` 같은 URI로 자원을 표현  |
| 2. HTTP 메서드 사용     | `GET`, `POST`, `PUT`, `DELETE` 등의 HTTP 메서드로 의도 표현 |
| 3. 무상태성(Stateless) | 서버는 요청 간의 상태를 저장하지 않음 (모든 요청은 독립적)                |
| 4. 계층 구조           | 중간 계층(예: 프록시, 캐시 등)이 개입해도 클라이언트는 모름               |
| 5. 클라이언트-서버 분리     | 프론트와 백엔드의 책임 명확히 분리                               |
| 6. 표현의 일관성         | 응답 데이터 포맷은 주로 JSON, XML 등을 사용 (보통 JSON)           |

1. 자원 중심 
2. HTTP 메서드로 행위 표현
3. 상태 유지 안 함