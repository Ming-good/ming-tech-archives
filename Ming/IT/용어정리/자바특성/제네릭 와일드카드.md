![[Pasted image 20250216122123.png]]
이 물음표가 와일드 카드이며, 물음표는 어떤 타입이든 될 수 있다는 뜻을 가지고 있다.

|     와일드 카드      |                   네이밍                    |                       설명                       |
| :-------------: | :--------------------------------------: | :--------------------------------------------: |
|      \<?\>      |   Unbounded wildcards  <br>비한정적 와일드 카드   |               제한 없음 (모든 타입이 가능)                |
| \<? extends U\> | Upper Bounded Wildcards  <br>상한 경계 와일드카드 | 상위 클래스 제한 (U와 그 자손들만 가능)<br>상한이 U라 상한 경계라고 한다. |
|  \<? super U>   | Lower Bounded Wildcards  <br>하한 경계 와일드카드 | 하위 클래스 제한 (U와 그 조상들만 가능)<br>하한이 U라 하한 경계라고 한다. |


## 상한 경계 와일드카드([[공변성]])
```java
class MyArrayList<T> {
    Object[] element = new Object[5];
    int index = 0;
    public MyArrayList(Collection<? extends T> in) {
        for(T elem : in) {
            element[index++] = elem;
        }
    }
}

public static void main(String[] args) {
    MyArrayList<Number> list;
    Collection<Integer> col = Arrays.asList(1, 2, 3, 4, 5);
    list = new MyArrayList<>(col);
    
    System.out.println(list); // [1, 2, 3, 4, 5]
}
```


## 하한 경계 와일드카드([[반공변성]])
![[Pasted image 20250216123541.png]]```
```java
class MyArrayList<T> {
    Object[] element = new Object[5];
    int index = 0;
    public MyArrayList(Collection<? extends T> in) {
        for (T elem : in) {
            element[index++] = elem;
        }
    }
    public void clone(Collection<? super T> out) {
        for (Object elem : element) {
            out.add((T) elem);
        }
    }

    @Override
    public String toString() {
        return Arrays.toString(element); 
    }
}

public static void main(String[] args) {
    MyArrayList<Number> list = new MyArrayList<>(Arrays.asList(1, 2, 3, 4, 5));

    List<Object> temp = new LinkedList<>();
    temp = list.clone(temp);

    System.out.println(temp); // [1, 2, 3, 4, 5]
}
```

## 비한정적 와일드카드
제네릭에 extend, super 범위를 제한하지 않고 심플하게 \<?\>비한정적 와일드카드로 구성하게되면 **어떠한 타입이도 올 수 있지만, 동시에 어떠한 타입이든 올 수 있는 문제**로 매개변수를 꺼내거나 저장하는 로직에서 **논리적 에러가 발생한다.**

![[Pasted image 20250216123943.png]]

---

## 와일드카드 제네릭 기능 정리

|표현|의미|읽기|쓰기|예시|
|---|---|---|---|---|
|`?`|어떤 타입이든 가능|Object로 읽기|`null`만 가능|`List<?>`|
|`? extends T`|T 또는 그 하위 타입|✅ (T로 읽기 가능)|❌ (쓰기 불가)|`List<? extends Number>`|
|`? super T`|T 또는 그 상위 타입|❌ (Object로만 읽힘)|✅ (T 넣기 가능)|`List<? super Integer>`|

## PECS 원칙 – Producer Extends, Consumer Super

| 역할                 | 제네릭 표현        | 설명                         |
| ------------------ | ------------- | -------------------------- |
| **Producer** (생산자) | `? extends T` | 컬렉션이 T를 **꺼내준다** (읽기만 가능)  |
| **Consumer** (소비자) | `? super T`   | 컬렉션이 T를 **받아들인다** (쓰기만 가능) |
> [[BlockingQueue]]와 생산자, 소비자 개념이 반대인 것처럼 보여서 혼동이 올 수 있다.
> BlockingQueue는 Queue를 중심으로 **데이터(스레드)가 주체**인
> 제네릭에서는 **컬렉션이 주체**이다.
#### 예시)
- **컬렉션 Producer → extends**:  읽기 →  컬렉션이 **값을 생산(제공)** 
- **컬렉션 Consumer → super**:  쓰기 → 컬렉션이 **값을 소비(저장)**

- **Queue Producer → put()** : 쓰기 → 큐에 값 **생산(저장)** 
- **Queue Consumer → take()** : 읽기 → 큐에서 **값 소비(제공)**
---
### 제네릭 사용시 주의사항
1. 제네릭 타입의 객체는 생성이 불가
	- new 연선 뒤에 제네릭이 올 수 없다.
```java
class Sample<T> {
    public void someMethod() {
        // Type parameter 'T' cannot be instantiated directly
        T t = new T();
    }
}
```
2. static 멤버에 제네릭 타입이 올 수 없음
```java
class Student<T> {
    private String name;
    private int age = 0;

    // static 메서드의 반환 타입으로 사용 불가
    public static T addAge(int n) {

    }
    // static 메서드의 매개변수 타입으로 사용 불가
    public static void addAge(T n) {

    }
}
```
3. 제네릭 배열 선언 주의
```java
class Sample<T> { 
}
****

public class Main {
    public static void main(String[] args) {
	    // 선언 불가능
        Sample<Integer>[] arr1 = new Sample<>[10];

	    // 선언 가능
        Sample<Integer>[] arr1 = new Sample[10];
    }
}
```