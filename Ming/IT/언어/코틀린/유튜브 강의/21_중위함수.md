```kotlin
fun main() {  
    println(5 plus 5)  
    println(5.plus(5))  
}  
  
infix fun Int.plus(x:Int):Int = this*x
```

## 사용 조건
- `infix` 키워드로 선언할 것.
- 멤버 함수 또는 확장 함수일 것.
- **매개변수는 반드시 1개**여야 함.