1. **`subscribe(consumer, errorConsumer, completeConsumer)`**
    - `consumer`: `onNext` 신호 수신 시 실행. (데이터 처리)
    - `errorConsumer`: `onError` 신호 수신 시 실행. (에러 처리)
    - `completeConsumer`: `onComplete` 신호 수신 시 실행. (종료 후처리)