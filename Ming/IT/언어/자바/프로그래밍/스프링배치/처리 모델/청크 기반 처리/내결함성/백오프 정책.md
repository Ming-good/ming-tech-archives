재시도 간격을 조정하려면 스텝에 BackOffPolicy를 설정해야 한다.
기본적으로 NoBackOffPolicy가 적용되며, 이 경우 실패즉시 재시도를 수행한다.

## `FixedBackOffPolicy`
```java
.retry(TerminationFailedException.class)
.retryLimit(3)
.backOffPolicy(new FixedBackOffPolicy() {{
  setBackOffPeriod(1000); // 1초
}})
```
## `ExponentialBackOffPolicy`
```java
```java
.retry(TerminationFailedException.class)
.retryLimit(3)
.backOffPolicy(new ExponentialBackOffPolicy() {{
  setInitialInterval(1000L);  // 초기 대기 시간
  setMultiplier(2.0);        // 대기 시간 증가 배수
  setMaxInterval(10000L);     // 최대 대기 시간
}})
```
![[Pasted image 20251103134923.png]]


