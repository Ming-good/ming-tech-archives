> `JdbcPaginItemReader`는 `Keyset`기반 페이징을 수행한다.

## 페이징 처리 개념
#### Offset 기반 페이징
- 데이터베이스가 결과셋을 정렬한 후, `OFFSET`만큼 건너뛰고 `LIMIT`개수만큼 데이터를 가져오는 방식이다.
	- 즉, `OFFSET + LIMIT`까지 데이터를 조회 후 `OFFSET`만큼 데이터를 버리기 때문에 불필요한 데이터 스캔을하는 문제가 있다.
- **Offset 기반 페이징의 문제점**
	- 불필요한 데이터를 조회하기 때문에 메모리를 많이 사용하고 조회 성능이 좋지 않다.
	- 페이지 번호가 뒬 갈수록 스캔하고 버려지는 데이터가 많아져서 성능이 급격히 저하된다.
#### Keyset 기반 페이징
- 이전 페이지의 마지막 키 값을 기준으로 그 다음 데이터를 가져온다
- 즉, 정확히 이전에 가져온 마지막 값 이후부터 읽어오므로 성능이 일정하게 유지된다.

## `JdbcPaginItemReader`의 구조
![[Pasted image 20251016145021.png]]
![[Pasted image 20251016145030.png]]
- **NamedParameterJdbcTemplate - SQL의 우아한 파라미터 처리** 
	- `JdbcTemplate`에서 한 단계 발전한 이 템플릿은 위치 기반 파라미터(?)대신 이름을 가진 파라미터를 사용할 수 있게 해준다.
- **PagingQueryProvider - 페이징 쿼리 생성의 전략가**
	- `JdbcPaginItemReader`는 Keyset 기반 페이징을 위한 특별한 쿼리 생성 도구를 필요로하며, 이를 위해 스프링 배치는 각 데이터베이스에 최적화된 `PaginQueryProvider`구현체를 제공한다.
		- PostgreSQL에서는 `PostgresPaginQueryProvider`를 제공
		- MySql에서는 `MySqlPaginQueryProvider`를 제공

## 사용 예시
```java
// SQL 자동구현
@Bean
public JdbcPagingItemReader<Victim> terminatedVictimReader() {
    return new JdbcPagingItemReaderBuilder<Victim>()
            .name("terminatedVictimReader")
            .dataSource(dataSource)
            .pageSize(5)
            .selectClause("SELECT id, name, process_id, terminated_at, status")
            .fromClause("FROM victims")
            .whereClause("WHERE status = :status AND terminated_at <= :terminatedAt")
            .sortKeys(Map.of("id", Order.ASCENDING))
            .parameterValues(Map.of(
                    "status", "TERMINATED",
                    "terminatedAt", LocalDateTime.now()
            ))
            .beanRowMapper(Victim.class)
            .build();
}

// SQL 수동 구현
@Bean
public JdbcPagingItemReader<Victim> terminatedVictimReader() {
    return new JdbcPagingItemReaderBuilder<Victim>()
            .name("terminatedVictimReader")
            .dataSource(dataSource)
            .pageSize(5)
            .queryProvider(pagingQueryProvider(dataSource)) // 커스텀 PagingQueryProvider 적용
            .parameterValues(Map.of(
                    "status", "TERMINATED",
                    "terminatedAt", LocalDateTime.now()
            ))
            .rowMapper(new BeanPropertyRowMapper<>(Victim.class))
            // .beanRowMapper(Victim.class)
            .build();
}

private PagingQueryProvider pagingQueryProvider(DataSource dataSource) {
    SqlPagingQueryProviderFactoryBean queryProviderFactory = new SqlPagingQueryProviderFactoryBean();

		// 데이터베이스 타입에 맞는 적절한 PagingQueryProvider 구현체를 생성할 수 있도록 dataSource를 전달해줘야 한다.
    queryProviderFactory.setDataSource(dataSource);
    queryProviderFactory.setSelectClause("SELECT id, name, process_id, terminated_at, status");
    queryProviderFactory.setFromClause("FROM victims");
    queryProviderFactory.setWhereClause("WHERE status = :status AND terminated_at <= :terminatedAt");
    queryProviderFactory.setSortKeys(Map.of("id", Order.ASCENDING));

    return queryProviderFactory.getObject();
}
```

