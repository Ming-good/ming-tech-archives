어떤 스레드를 얼마나 실행할지는 운영체제가 스케줄링을 통해 결정한다. 스레드 작업을 양보하면 스케줄링 큐에 대기 중인 다른 스레드가 CPU 실행 기회를 더 빨리 얻을 수 있다.

### 스레드 작업 양보 방식
1. Empty :  sleep(1), yield() 없이 호출한다. 운영체제의 스레드 스케줄링을 따른다.
2. **sleep(1)** : 특정 스레드를 잠시 쉬게 한다. (특정 시간 동안 스레드가 실행되지 않는 단점이 있다)
3. **yield()** : yield() 를 사용해서 다른 스레드에 실행을 양보한다.

### 자바의 스레드가 RUNNABLE 상태일 때 OS의 스케줄링 상태
1. **실행 상태(Running)**: 스레드가 CPU에서 실제로 실행중이다
2. **실행 대기 상태(Ready)**: 스레드가 실행될 준비가 되었지만, CPU가 바빠서 스케줄링 큐에서 대기 중이다
> 자바는 두 상태를 구분할 수 없다. (위의 상태값은 운영체제 상태값이다)

### yield()의 작동
- **Thread.yield()** : 현재 실행중인 스레드가 자발적으로 CPU를 양보하여 다른 스레드가 실행 될 수 있게 한다.
- **yield()** : yield를 호출한 스레드는 RUNNABLE 상태를 유지하면서 CPU를 양보한다.
	- 즉, 이 스레드는 다시 스케줄링 큐에 들어가면서 다른 스레드에게 CPU 사용 기회를 넘긴다.
> 자바에서 Thread.yield() 메서드를 호출하면 스레드가 CPU를 양보하도록 **힌트**를 준다. 
> 이것은 단지 힌트일 뿐, **강제적인 실행 순서를 지정하지는 않는다**.

![[Pasted image 20250427163344.png]]

![[Pasted image 20250427163400.png]]

#### yield()
**실행 결과3 - yield**
```
Thread-805 - 9
Thread-321 - 9
Thread-880 - 8
Thread-900 - 8Thread-900 - 9
Thread-570 - 9
Thread-959 - 9
Thread-818 - 9
Thread-880 - 9