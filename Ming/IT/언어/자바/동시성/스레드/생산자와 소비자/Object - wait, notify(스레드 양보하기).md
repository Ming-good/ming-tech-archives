#### `Object.wait()`
- 현재 스레드가 가진 락을 반납하고 대기(`WAITING` )한다. (다른 스레드가 해당 락을 획득 할 수 있다.)
- 현재 스레드를 대기(`WAITING`) 상태로 전환한다. 해당 스레드는 다른 스레드가 `notify()`또는`notifyAll()`을 호출할 때 까지 **대기 상태를 유지**한다
>  이 메서드는 현재 스레드가 `synchronized` 블록이나 메서드에서 락을 소유하고 있을 때만 호출할 수 있다. .