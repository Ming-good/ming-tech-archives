## 특징
- Extensible  확장 가능 
- Simple  간단합니다 
- Efficient  효율적이다 
- Portable  포터블 
- Free and open  자유롭고 개방된 공간

## 루아의 용도
- 게임 프로그래밍
- 독립 실행형 애플리케이션에서의 스크립팅
- 웹에서의 스크립팅
- MySQL Proxy와 MySQL WorkBench 같은 데이터베이스용 확장 및 애드온
- 침입 탐지 시스템(Intrusion Detection System) 같은 보안 시스템

```lua
a=3
if a==3 then
	print("안녕하세요".." `..`은 문자 붙이기 입니다")
	local a = {"a", "b", "c", "d"}
	print(#a.. " #배열은 유형의 아이템 갯 입니다")
else if a==4
	then
		print(2)
	else
		print(3)
	end
end


while(a < 20)
do 
	print("반복문 입니다", a)
	a = a+1
end

for i = 10, 20, 1
do
	print(i)
end

test=function (a,b)
	print("익명 함수 입니다.")
	return 123
end

  
function funfunc(num1, num2,fun)
	print("함수를 넘겼습니다")
	print(fun(num1,num2))
end
funfunc(1, 2, test)

print(type(tonumber("1")))
print(string.upper("abcdabcd"))

arr = {1,2,3,4,5}
for i=1, #arr
do
	print(arr[i].."이애는 for문이 1부터 시작이다")
end
```