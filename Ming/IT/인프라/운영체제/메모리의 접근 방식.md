## 멀티스레드 환경에서 메모리에 접근하는 일반적인 시나리오
![[Pasted image 20250427184959.png]]
- 멀티스레드 환경에서  변수 값을 변경, 로드시 일반적으로 메인 메모리를 통해 즉시 교체될 것으로 생각한다.
- 하지만 멀티스레드 환경에서 값이 변경되지 않는 문제를 볼 수 있다. 
---

## 실제 메모리의 접근 방식 - [[메모리 가시성(memory visibility)]]
![[Pasted image 20250427185642.png]]
![[Pasted image 20250427185356.png]]
> CPU는 처리 성능을 개선하기 위해 중간에 **캐시 메모리**라는 것을 사용한다.
- 메인 메모리는 **CPU에서 멀고 느리지만**, **용량이 크고 가격이 저렴**하다.
- **캐시 메모리**는 **속도가 빠르고 CPU에 가깝지만**, **용량이 작고 가격이 비싸다**.
- CPU는 연산 속도가 매우 빨라서, 이를 따라가기 위해 **CPU 가까이에 빠른 메모리**가 필요한데, 이게 **캐시 메모리**다.
- 현대 CPU는 **코어별로 캐시 메모리를 보유**하며, **여러 코어가 공유하는 캐시**도 있다.

### 캐시 메모리 🔛 메인메모리 반영,로드 시점
- 캐시 메모리에 있는 `runFlag` 의 값이 언제 메인 메모리에 반영될까?
> 이 부분에 대한 정답은 "알 수 없다"이다.  CPU 설계 방식과 종류의 따라 다르다. 
> (메인 메모리에 값이 캐시 메모리로 반영되는 시기 또한 알 수 없다)

- 주로 [[컨텍스트 스위칭]]이 될 때, 캐시 메모리도 함께 갱신되는데, 이 부분도 환경에 따라 달라질 수 있다.
> 예를 들어 `Thread.sleep()` 이나 콘솔에 내용을 출력할 때 스레드가 잠시 쉬는데, 
> 이럴 때 컨텍스트 스위칭이 되면서 주로 갱신된다. 하지만 이것이 갱신을 보장하는 것은 아니다.
